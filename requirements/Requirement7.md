# Exclude

Before copying in a mountobserver script element, check if the custom element has property "exclude" or attribute "exclude" and if so, see if the script element matches that value, and if so, don't append the element